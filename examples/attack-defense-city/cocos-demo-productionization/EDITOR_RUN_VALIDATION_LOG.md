# Editor Run Validation Log

Use this template after opening the local Cocos Creator 3.8.8 project.

## Project info

```text
Cocos Creator version:
Project path:
Scene name:
Date:
Tester:
```

## Repository validation

```text
npm run validate:example:
npm run export:example:
npm run types:example:
npm run validate:runtime:
npm run check:
```

## Cocos script validation

- [ ] scripts copied into `assets/scripts/attack-defense-city/`
- [ ] Cocos script compilation finished
- [ ] no missing import error
- [ ] no missing component error
- [ ] components visible in Inspector

Notes:

```text

```

## Scene validation

- [ ] `scene_city_battle` opens
- [ ] `GameRoot` exists
- [ ] `SpawnPoints` exists
- [ ] map point nodes exist
- [ ] `EnemyRoot` exists
- [ ] `UIRoot` exists

Notes:

```text

```

## Inspector binding validation

- [ ] `AttackDefenseCityBootstrap` attached
- [ ] `mapPoints` assigned
- [ ] `enemyScoutPrefab` assigned
- [ ] `enemyRaiderPrefab` assigned
- [ ] `enemyRoot` assigned

Notes:

```text

```

## First run result

```text
Did the scene run:
Console output:
Errors:
Warnings:
```

## Decision

Choose one:

```text
PASS
PASS WITH FIXES
BLOCKED
```

## Next action

```text

```
